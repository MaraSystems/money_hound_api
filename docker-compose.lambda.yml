services:

  api:
    image: ${ECR_REPOSITORY}:${IMAGE_TAG}
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile.lambda
    ports:
      - "8080:8080"
    environment:
      AWS_LAMBDA_RUNTIME_API: localhost:8080
